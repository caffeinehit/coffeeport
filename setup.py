#!/usr/bin/env python

from setuptools import setup, find_packages
import coffeeport

setup(
    name='coffeeport',
    version=coffeeport.__version__,
    description='',
    url='',
    author='',
    author_email='',
    packages=find_packages(),
    include_package_data=True,
    install_requires = [],
)
